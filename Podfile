# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NJ-Transit' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod "Microfutures"
  # Pods for NJ-Transit

  target 'NJ-TransitTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'NJ-TransitUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
